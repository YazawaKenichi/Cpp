#include "mbed.h"
#include "adrobo.h"

BusOut led(LED1,LED2,LED3,LED4,LED5,LED);
/*
	BusOut busname(pin1, pin2, pin3, pin4, ...);
	pin1 ~ pinN ???????????????????
	???????????? I/O ????????????????
	??? pin1, pin2, pin3, pin4 ???? 4bit ????????????????
*/

int main() {
    int no = 0;
    while(1) {
        led = 1 << no; //led ? 1 ? no ?????????????? led ??????
        no = (no >= 5) ? 0 : (no + 1); //no ? 4 ???? +1 ????5 ??????????
        wait(0.5);
    }
}
